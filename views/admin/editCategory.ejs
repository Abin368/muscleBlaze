<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Category</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/customerDetails.css">
    <link rel="stylesheet" href="/editcategoryDetails.css">
</head>
<body>
    <div class="container">
        <h2>Edit Category</h2>

        <% if (typeof message !== 'undefined' && message) { %>
            <div class="alert alert-danger"><%= message %></div>
        <% } else if (typeof error !== 'undefined' && error) { %>
            <div class="alert alert-danger"><%= message %></div>
        <% } %>

        <form action="/admin/updateCategory" method="POST">
            <input type="hidden" name="id" value="<%= category._id %>">

            <div class="input-group">
                <label for="name">Category Name</label>
                <input type="text" id="name" name="name" value="<%= category.name %>" required>
            </div>

            <div class="input-group">
                <label for="description">Description</label>
                <textarea id="description" name="description" required><%= category.description %></textarea>
            </div>

            <button type="submit">Update</button>
            <a href="/admin/categories" class="cancel-btn">Cancel</a>
        </form>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>


</html>
